# https://www.robotstxt.org/robotstxt.html
#
# LAMIKAA NATURALS — the storefront is open to crawlers; the account, checkout
# and admin surfaces are not. Every path below is either private to one visitor
# (orders, profile, a cart, an order confirmation) or a step in a transaction.
# The pages themselves also carry `noindex` where it matters
# (useSeo), because a Disallow only stops the crawl, not the indexing of a URL
# someone else links to.
User-agent: *
Allow: /
Disallow: /admin
Disallow: /checkout
Disallow: /order-confirmation
Disallow: /profile
Disallow: /orders
Disallow: /cart

# NO Sitemap: LINE ON PURPOSE. It pointed at the LAMIKAA_DOMAIN placeholder
# until Prompt 36: brand.seo.siteUrl is still unresolved, robots.txt is served
# verbatim to crawlers, and a literal placeholder there is a broken directive —
# worse than no directive at all. Prompt 38's generate-sitemap script writes
# public/sitemap.xml only once the owner names the domain, and re-adds this line
# with the real host at the same time.
